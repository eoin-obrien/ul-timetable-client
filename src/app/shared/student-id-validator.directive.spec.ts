import { StudentIdValidatorDirective } from './student-id-validator.directive';

describe('StudentIdValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new StudentIdValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
